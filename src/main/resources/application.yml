spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/freevoice
    username: gandalf_the_blue
    password: postgres

  jpa:
    hibernate:
      ddl-auto: create
#    show-sql: true

  servlet:
    multipart:
      enabled: true
      file-size-threshold: 2KB
      max-file-size: 900MB
      max-request-size: 915MB

server:
  error:
    include-message: always

  mail:
    host: 172.17.0.3
    port: 25
    username: user01@james.local
    password: 1234
    properties:
      mail:
        smtp:
          ssl:
            trust: "*"
          auth: true
          starttls:
            enable: true
          connectiontimeout: 10000
          timeout: 10000
          writetimeout: 10000
